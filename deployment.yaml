kind: Service
apiVersion: v1
metadata:
  name: qms-backstage-web
spec:
  selector:
    app: qms-backstage
  type: NodePort
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: qms-backstage
  labels:
    app: qms-backstage
spec:
  replicas: 1
  selector:
    matchLabels:
      app: qms-backstage
  template:
    metadata:
      labels:
        app: qms-backstage
    spec:
      imagePullSecrets:
      - name: pipeline-docker-registry
      containers:
      - name: maven
        image: ${CICD_IMAGE}:${CICD_EXECUTION_SEQUENCE}
        ports:
        - containerPort: 8080
